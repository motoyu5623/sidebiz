  <div class="field">
    <span class="field-left"><%= f.label '比較対象となる本業' %></span>
    <span class="field-right"><%= f.collection_select :main_job_id, current_user.main_jobs.all, :id, :name, {}, class:"input-form-select" %></span>
  </div>

  <div class="field">
    <span class="field-left"><%= f.label '本業から活かせたこと' %></span>
    <span class="field-right"><%= f.text_area :pulled_skill, class:"input-form-large" %></span>
  </div>

  <div class="field">
    <span class="field-left"><%= f.label '本業へ還元できたこと' %></span>
    <span class="field-right"><%= f.text_area :returned_skill, class:"input-form-large" %></span>
  </div>

  <hr>
  <div class="field">
    <span class="field-left">重要なスキル</span>
    <span class="field-right">
      <small>副業において最も重要と考えられるスキルを、3つご記入ください。<br>そのスキルについて、副業と本業、それぞれにおける重要度を選択してください。</small>
      <table border="1" style="border-collapse: collapse" class="skill-input-table mt-2">
        <tr>
          <th>スキル名</th>
          <th>副業での重要度</th>
          <th>(参考)本業での重要度</th>
        </tr>
        <%= f.fields_for :skills do |skill_field| %>
        <tr>
          <td><%= skill_field.text_field :name, class:"form-in-table" %></td>
          <td><%= skill_field.select(:importance_for_side_job, options_for_select([['1: 全く重要でない', 1],['2: あまり重要でない', 2],['3: まあまあ重要', 3],['4: 重要', 4],['5: 非常に重要', 5]], selected: skill_field.object.importance_for_side_job), {}, class:"form-in-table") %></td>

          <td><%= skill_field.select(:importance_for_main_job, options_for_select([['1: 全く重要でない', 1],['2: あまり重要でない', 2],['3: まあまあ重要', 3],['4: 重要', 4],['5: 非常に重要', 5]], selected: skill_field.object.importance_for_side_job), {}, class:"form-in-table") %></td>

        </tr>
        <% end %>
      </table>
    </span>
  </div>