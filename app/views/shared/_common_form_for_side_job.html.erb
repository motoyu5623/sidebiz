  <div class="field">
    <span class="field-left"><%= f.label '比較対象となる本業' %></span>
    <span class="field-right"><%= f.collection_select :main_job_id, current_user.main_jobs.all, :id, :name, {}, class:"input-form-select" %></span>
  </div>

  <div class="field">
    <span class="field-left"><%= f.label '本業から活用できたスキル' %></span>
    <span class="field-right"><%= f.text_area :pulled_skill, class:"input-form-large" %></span>
  </div>

  <div class="field">
    <span class="field-left"><%= f.label '本業に還元できたスキル' %></span>
    <span class="field-right"><%= f.text_area :returned_skill, class:"input-form-large" %></span>
  </div>

  <hr>
  <div class="field">
    <span class="field-left">重要なスキル</span>
    <span class="field-right">
      <table border="1" style="border-collapse: collapse" class="skill-input-table">
        <tr>
          <th>スキル名</th>
          <th>副業での重要度</th>
          <th>本業での重要度</th>
        </tr>
        <%= f.fields_for :skills do |skill_field| %>
        <tr>
          <td><%= skill_field.text_field :name, class:"form-in-table" %></td>
          <td><%= skill_field.select(:importance_for_side_job, options_for_select([['1: 全く重要でない', 1],['2: あまり重要でない', 2],['3: まあまあ重要', 3],['4: 重要', 4],['5: 非常に重要', 5]]), {}, class:"form-in-table") %></td>

          <td><%= skill_field.select(:importance_for_main_job, options_for_select([['1: 全く重要でない', 1],['2: あまり重要でない', 2],['3: まあまあ重要', 3],['4: 重要', 4],['5: 非常に重要', 5]]), {}, class:"form-in-table") %></td>

        </tr>
        <% end %>
      </table>
    </span>
  </div>